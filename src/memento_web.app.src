
{application, memento_web, [
	{description, "qooxdoo in erlang."},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, [memw_sup]},
	{applications, [
		kernel,
		stdlib,
        compiler,
        lager,
		crypto,
		public_key,
		ssl,
		cowboy,
        cowboy_static,
        bullet,
        jsx
	]},
    {mod, {memw_app, []}},
	{env, []}
]}.
